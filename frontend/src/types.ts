export type lessonSlot = {
  name: string,
  branch: string,
};

export type TeacherData = {
  id: string;
  name: string;
  branch: string;
  placeholder: string;
};

export type ClassData = {
  id: string;
  year: number;
  class: string;
};